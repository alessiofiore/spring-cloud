spring:
  application:
    name: discovery
  cloud:
    config:
      uri: http://localhost:8060
      failFast: true
      retry:
        initial-interval: 2000
        multiplier: 1.5
        max-attempts: 30
      