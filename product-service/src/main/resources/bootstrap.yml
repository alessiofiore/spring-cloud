spring:
  application:
    name: product-service
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8070/eureka/
        
server:
  port: ${PORT:9090}
 
