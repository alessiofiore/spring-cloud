spring:
  application:
    name: order-service
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8070/eureka/
        
server:
  port: ${PORT:9060}
 
