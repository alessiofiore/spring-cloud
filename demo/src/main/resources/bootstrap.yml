spring:
  application:
    name: client
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config 

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8082/eureka/
        
server:
  port: ${PORT:9090}
 
