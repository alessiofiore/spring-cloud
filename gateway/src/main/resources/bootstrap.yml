spring:
  application:
    name: gateway
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8070/eureka/
      registerWithEureka: false
        
server:
  port: ${PORT:8080}
 
